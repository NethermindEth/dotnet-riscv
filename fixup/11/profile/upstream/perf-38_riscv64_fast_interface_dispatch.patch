.NET 11 replaced the per-cell cached interface dispatch stubs with a dispatch
helper (dotnet/runtime#123252): the cell is { cached MethodTable, cached code }
and polymorphic call sites are cached in a global hashtable. amd64/arm64 got
optimized helpers with an inline monomorphic check and an asm hashtable probe;
riscv64 only got the slow helper that goes through RhpUniversalTransition and
the managed resolver on *every* interface call (RhpCidResolve_Worker ->
FindImplSlotInSimpleMap show up at the top of zkVM step profiles, ~2x the
total step count of .NET 10 on the same guest).

Port the arm64 INTERFACE_DISPATCH macro to riscv64: monomorphic cell check,
then a GenericCache<Key, nint> probe (same hash, quadratic reprobe, seqlock
version check), falling back to RhpCidResolve / RhpCidResolve_Worker. Only
temporaries are used, so no argument spilling; the probe count is packed into
the upper bits of the index register because riscv64 has exactly one spare
temporary at the key compare. Plain loads are ordered with "fence r, r" where
arm64 relies on ldar/dmb (a nop-cost instruction on single-hart zkVMs).

diff --git a/src/coreclr/nativeaot/Runtime/riscv64/DispatchResolve.S b/src/coreclr/nativeaot/Runtime/riscv64/DispatchResolve.S
--- a/src/coreclr/nativeaot/Runtime/riscv64/DispatchResolve.S
+++ b/src/coreclr/nativeaot/Runtime/riscv64/DispatchResolve.S
@@ -4,35 +4,138 @@
 #include <unixasmmacros.inc>
 
 // Dispatching version of RhpResolveInterfaceMethod
-LEAF_ENTRY RhpInterfaceDispatch, _TEXT
+.macro INTERFACE_DISPATCH DispatchName, ReturnTarget, DispatchCellReg
 
+LEAF_ENTRY \DispatchName, _TEXT
+
         // Load the MethodTable from the object instance in a0.
         // Trigger an AV if we're dispatching on a null this.
         // The exception handling infrastructure is aware of the fact that this is the first
         // instruction of RhpInterfaceDispatch and uses it to translate an AV here
         // to a NullReferenceException at the callsite.
-        ld      zero, 0(a0)
+        ld      t0, 0(a0)
 
-        // t5 currently contains the indirection cell address.
+        // DispatchCellReg currently contains the indirection cell address.
+        // The cell is { cached MethodTable, cached code }. The writer stores Code
+        // before MethodTable (release), so the load of Code has to be ordered after
+        // the load of MethodTable: RVWMO may reorder two plain loads, hence the fence.
+        ld      t6, 0(\DispatchCellReg)
+        fence   r, r
+        bne     t6, t0, LOCAL_LABEL(Hashtable\@) // not the monomorphic MethodTable?
+
+        ld      t6, 8(\DispatchCellReg)         // cached monomorphic resolved code address
+.if \ReturnTarget
+        mv      a0, t6
+        ret
+.else
+        jr      t6
+.endif
+
+LOCAL_LABEL(Hashtable\@):
+
+        // t0 = MethodTable, DispatchCellReg = indirection cell address
+        // Look up the target in the dispatch cache hashtable (GenericCache<Key, nint>).
+        // Only the temporaries t1-t4/t6 are used, so no argument register spilling is needed.
+        //
+        // Register use in the probe loop:
+        //   t1 = Entry[] table, t2 = bucket index (bits 0..31) | probe count i (bits 40..47),
+        //   t3 = current entry, t4 = version snapshot, t6 = scratch.
+
+        // Load the _table field (Entry[]) from the cache struct.
+        PREPARE_EXTERNAL_VAR g_pDispatchCache, t1
+        ld      t1, 0(t1)
+        ld      t1, 0(t1)
+
+        // Compute 32-bit hash from Key.GetHashCode():
+        // hash = IntPtr.GetHashCode(RotateLeft(dispatchCell, 16) ^ objectType)
+        slli    t2, \DispatchCellReg, 16
+        srli    t3, \DispatchCellReg, 48
+        or      t2, t2, t3              // RotateLeft(cell, 16)
+        xor     t2, t2, t0              // combined = RotateLeft(cell, 16) ^ MethodTable
+        srai    t3, t2, 32              // upper32 = combined >> 32 (arithmetic)
+        xor     t2, t2, t3              // hash = (int)lower32 ^ (int)upper32
+        sext.w  t2, t2                  // sign-extend hash to 64-bit
+
+        // HashToBucket: bucket = (hash * 0x9E3779B97F4A7C15) >> hashShift
+        li      t3, -7046029254386353131 // 0x9E3779B97F4A7C15
+        mul     t2, t2, t3
+        lbu     t3, 0x10(t1)            // hashShift from Element[0]._info
+        srl     t2, t2, t3              // bucket index, i = 0
+
+LOCAL_LABEL(ProbeLoop\@):
+        // Compute entry address: Element(table, index) = table + 0x10 + (index + 1) * 0x20
+        slli    t3, t2, 32              // drop the probe count, keep index in the upper half
+        srli    t3, t3, 27              // index * 0x20
+        add     t3, t3, t1
+        addi    t3, t3, 0x30
+
+        // Read version snapshot (seqlock protocol). The fence orders the loads of the
+        // key and value after the load of the version.
+        lw      t4, 0(t3)
+        fence   r, r
+        andi    t6, t4, 1
+        bnez    t6, LOCAL_LABEL(ProbeMiss\@) // odd: entry being written
+
+        // Compare key (dispatchCell, objectType)
+        ld      t6, 8(t3)
+        bne     t6, \DispatchCellReg, LOCAL_LABEL(ProbeMiss\@)
+        ld      t6, 16(t3)
+        bne     t6, t0, LOCAL_LABEL(ProbeMiss\@)
+
+        // Read the cached code pointer.
+        ld      t6, 24(t3)
+
+        // Ensure value read completes before version re-read.
+        fence   r, r
+
+        // Re-verify version has not changed.
+        lw      t3, 0(t3)
+        bne     t3, t4, LOCAL_LABEL(CacheMiss\@)
+
+        // Dispatch to cached target.
+.if \ReturnTarget
+        mv      a0, t6
+        ret
+.else
+        jr      t6
+.endif
+
+LOCAL_LABEL(ProbeMiss\@):
+        // If version is zero the rest of the bucket is unclaimed - stop probing.
+        beqz    t4, LOCAL_LABEL(CacheMiss\@)
+
+        // Quadratic reprobe: i++; index = (index + i) & tableMask
+        srli    t6, t2, 40              // i
+        addi    t6, t6, 1
+        li      t3, 8
+        bge     t6, t3, LOCAL_LABEL(CacheMiss\@)
+        slli    t3, t2, 32
+        srli    t3, t3, 32              // index
+        add     t3, t3, t6              // index + i
+        lw      t4, 8(t1)               // table.Length
+        addi    t4, t4, -2              // TableMask = Length - 2
+        and     t3, t3, t4
+        slli    t6, t6, 40
+        or      t2, t3, t6              // repack index | i << 40
+        j       LOCAL_LABEL(ProbeLoop\@)
+
+LOCAL_LABEL(CacheMiss\@):
+.if \ReturnTarget
+        tail    C_FUNC(RhpCidResolve_Worker)
+.else
         // Calling convention of the universal thunk is:
         //  t0: target address for the thunk to call
         //  t1: parameter of the thunk's target
         PREPARE_EXTERNAL_VAR RhpCidResolve, t0
-        mv      t1, t5
+        mv      t1, \DispatchCellReg
 1:
         auipc   t2, %pcrel_hi(C_FUNC(RhpUniversalTransitionTailCall))
         jalr    x0, t2, %pcrel_lo(1b)
+.endif
 
-LEAF_END RhpInterfaceDispatch, _TEXT
+LEAF_END \DispatchName, _TEXT
 
-// Resolve helper for the standard calling convention.
-// a0 = object instance, a1 = indirection cell address, result in a0.
-LEAF_ENTRY RhpDispatchResolve, _TEXT
+.endm
 
-        // Load the MethodTable from the object instance in a0.
-        // Trigger an AV if we're dispatching on a null this.
-        ld      t0, 0(a0)
-
-        tail    C_FUNC(RhpResolveInterfaceMethod)
-
-LEAF_END RhpDispatchResolve, _TEXT
+        INTERFACE_DISPATCH RhpInterfaceDispatch, 0, t5
+        INTERFACE_DISPATCH RhpDispatchResolve, 1, a1
